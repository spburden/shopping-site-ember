<ul>
  {{#each items as |item|}}
    <h2>{{#link-to 'item' item.id}}{{item.name}}{{/link-to}}</h2>
    <p>{{item.price}}</p>
    <p>{{item.description}}</p>
    <p>{{item.quantity}}</p>
    <img src={{item.image}}>
    <br>
  {{/each}}
</ul>
