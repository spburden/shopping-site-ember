
import { itemQuantity } from 'shopping-site/helpers/item-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | item quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemQuantity([42]);
  assert.ok(result);
});

